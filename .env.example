# Redis
REDIS_URL=redis://localhost:6379

# Scanner auth — shared secret the backend must send as X-Scanner-Secret header
# Leave empty to disable auth (local dev)
SCANNER_SECRET=

# Worker concurrency — how many jobs run simultaneously
WORKER_CONCURRENCY=5

# Set to true to disable rate limiting (load testing only)
LOAD_TEST_MODE=false
