diff --git a/node_modules/lighthouse/core/gather/driver/prepare.js b/node_modules/lighthouse/core/gather/driver/prepare.js
index 4f3c96d..91f204b 100644
--- a/node_modules/lighthouse/core/gather/driver/prepare.js
+++ b/node_modules/lighthouse/core/gather/driver/prepare.js
@@ -143,7 +143,12 @@ async function prepareThrottlingAndNetwork(session, settings) {
  */
 async function prepareDeviceEmulation(driver, settings) {
   // Enable network domain here so future calls to `emulate()` don't clear cache (https://github.com/GoogleChrome/lighthouse/issues/12631)
-  await driver.defaultSession.sendCommand('Network.enable');
+  // Raise the response-body buffers so large pages don't evict the main document
+  // body before it's read (charset audit errors -> null best-practices).
+  await driver.defaultSession.sendCommand('Network.enable', {
+    maxTotalBufferSize: 1000000000,
+    maxResourceBufferSize: 200000000,
+  });
 
   // Emulate our target device screen and user agent.
   await emulation.emulate(driver.defaultSession, settings);
diff --git a/node_modules/lighthouse/core/gather/driver/target-manager.js b/node_modules/lighthouse/core/gather/driver/target-manager.js
index 84ab913..f522cdd 100644
--- a/node_modules/lighthouse/core/gather/driver/target-manager.js
+++ b/node_modules/lighthouse/core/gather/driver/target-manager.js
@@ -159,7 +159,12 @@ class TargetManager extends ProtocolEventEmitter {
       this._targetIdToTargets.set(targetId, targetWithSession);
 
       // We want to receive information about network requests from iframes, so enable the Network domain.
-      await newSession.sendCommand('Network.enable');
+      // Raise the response-body buffers so large pages don't evict the main document
+      // body before the MainDocumentContent gatherer reads it (charset audit errors -> null best-practices).
+      await newSession.sendCommand('Network.enable', {
+        maxTotalBufferSize: 1000000000,
+        maxResourceBufferSize: 200000000,
+      });
       // We also want to receive information about subtargets of subtargets, so make sure we autoattach recursively.
       await newSession.sendCommand('Target.setAutoAttach', {
         autoAttach: true,
